version: 1
frontend:
  phases:
    preBuild:
      commands:
        - echo "Starting build process..."
    build:
      commands:
        - echo "Building HomeOrder application..."
        - echo "No build commands needed for static HTML/CSS/JS"
  artifacts:
    baseDirectory: /
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/* 